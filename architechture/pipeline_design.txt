INPUT FOLDER (GDrive)
│
├── file1.xlsx
├── file2.xlsx
└── file3.xlsx
        │
        ▼
┌─────────────────────┐
│   Layer 1: INGEST   │
│   list_files        │
│   download_file     │
│   concat → raw_df   │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ Layer 2: TRANSFORM  │
│   clean             │
│   standardise       │
│   build_unique_key  │
│   → transformed_df  │
└────────┬────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│              Layer 3: DATE ROUTER                       │
│                                                         │
│   group transformed_df by month from date column        │
│                                                         │
│   for each month group:                                 │
│         │                                               │
│         ▼                                               │
│   get_or_create_folder → YYYY-MM                        │
│   get_or_create_gsheet → YYYY-MM_raw                    │
│   read_gsheet_as_df    → existing_df                    │
│         │                                               │
│         ├── existing_df EMPTY ──→ upload_df_as_gsheet   │
│         │                        (fresh write)          │
│         │                                               │
│         └── existing_df HAS DATA                        │
│                   │                                     │
│                   ▼                                     │
│             anti_join(new, existing)                    │
│                   │                                     │
│                   ├── delta EMPTY → skip                │
│                   │                                     │
│                   └── delta HAS ROWS                    │
│                         │                               │
│                         ▼                               │
│                   append_rows_to_gsheet                 │
└─────────────────────────────────────────────────────────┘
         │
         ▼
OUTPUT FOLDER (GDrive)
│
├── 2026-04/
│     └── report_2026-04  (GSheet)
│
├── 2026-05/
│     └── report_2026-05  (GSheet)
│
└── 2026-06/
      └── report_2026-06  (GSheet)
         │
         ▼
┌──────────────────────────┐
│  Layer 4: CONSOLIDATION  │  ← separate scheduled job
│  read all monthly sheets │
│  stack → master_df       │
│  write → dashboard sheet │
└──────────────────────────┘

